/* ==========================================================================
   JOB ASSIGNMENT: NARENDRA
   TASK: JavaScript Logic - Video Playlist (video.js)
   ==========================================================================
   INSTRUCTIONS FOR NARENDRA:
   1. Muat daftar video secara dinamis dari array 'videoMateri' di data.js.
   2. Render daftar playlist video di panel playlist samping/bawah:
      - Setiap playlist item menampilkan indeks angka atau ikon, beserta judul video.
      - Klik item playlist untuk memutar video tersebut di pemutar utama.
   3. Implementasikan fungsi pemutar video:
      - Setel atribut '.src' pada elemen `<video>` dengan path video lokal (.mp4).
      - Panggil `.load()` pada video player saat mengganti file video untuk memicu refresh data browser.
      - Perbarui judul dan deskripsi pada info card sesuai video yang sedang diputar.
   4. Berikan penanda visual (kelas '.active') pada item playlist yang sedang diputar saat ini.
   5. Sinkronisasikan UDL Toolbar (Zoom Font, Dyslexia Font) dari localStorage agar konsisten saat halaman dimuat.
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
    // Tulis kode logika playlist video Anda di sini
});
